🔹 1. Список (list)

Це впорядкована змінна колекція елементів.

Елементи можна змінювати (mutable).

Дозволяє дублікати.

Приклад:

fruits = ["яблуко", "груша", "банан", "яблуко"]
print(fruits[0])       # доступ по індексу → "яблуко"
fruits.append("слива") # додає елемент
print(fruits)


👉 Списки — найчастіше використовувана структура для зберігання послідовностей.

🔹 2. Множина (set)

Це неупорядкована колекція унікальних елементів.

Дублікати автоматично видаляються.

Використовується для перевірки унікальності або для операцій з множинами (перетин, об’єднання).

Приклад:

numbers = {1, 2, 3, 2, 4}
print(numbers)              # {1, 2, 3, 4} — дублікати зникли
numbers.add(5)              # додає новий елемент
print(3 in numbers)         # True

🔹 3. Кортеж (tuple)

Це впорядкована колекція, але незмінна (immutable).

Використовується для даних, які не повинні змінюватися.

Може містити різні типи.

Приклад:

point = (10, 20)
print(point[0])  # 10
# point[0] = 5   # ❌ помилка — кортеж незмінний

🔹 4. Словник (dict)

Це колекція пар ключ:значення.

Ключі унікальні, значення можуть повторюватися.

Використовується для швидкого доступу до даних за ключем.

Приклад:

student = {
    "ім'я": "Олег",
    "вік": 20,
    "курс": 2
}
print(student["ім'я"])       # "Олег"
student["вік"] = 21          # змінюємо значення
student["факультет"] = "IT"  # додаємо новий ключ

🔹 Порівняння:
Тип	Впорядкований	Змінний	Дублікати	Приклад
Список	✅	✅	✅	[1,2,2,3]
Множина	❌	✅	❌	{1,2,3}
Кортеж	✅	❌	✅	(1,2,2,3)
Словник	✅ (Python 3.7+)	✅	❌ (ключі)	{"a":1,"b":2}

📌 Отже, списки зручні для роботи з послідовностями,
множини — для унікальних значень і математичних операцій,
кортежі — для незмінних структур,
словники — для даних із ключами.

Хочеш, я складу приклади практичного використання всіх чотирьох у реальному маленькому завданні (наприклад, міні-програма для зберігання студентів і їхніх оцінок)?

Ви сказали:
так зроби але додай ще до них методи
Програма ChatGPT сказала:

Добре 👍 давай зробимо невеликий міні-проєкт, де використаємо список, множину, кортеж і словник, а паралельно покажу основні методи кожної структури.

🎓 Міні-програма: “Студенти та їхні оцінки”

Уявімо, що ми ведемо облік студентів, їхніх оцінок і факультетів.

# =============================
# 1. С П И С О К  (list)
# =============================

# Список студентів
students = ["Олег", "Марія", "Іван"]

# Основні методи:
students.append("Катя")   # додає елемент в кінець
students.insert(1, "Андрій") # вставляє на позицію
students.remove("Іван")   # видаляє по значенню
students.pop()            # видаляє останній елемент
students.sort()           # сортує список
students.reverse()        # розвертає список
print("Список студентів:", students)


# =============================
# 2. М Н О Ж И Н А  (set)
# =============================

# Множина факультетів
faculties = {"IT", "Математика", "Фізика", "IT"}  # дублікати зникнуть

# Основні методи:
faculties.add("Біологія")      # додає елемент
faculties.remove("Фізика")     # видаляє (якщо нема — помилка)
faculties.discard("Хімія")     # видаляє без помилки
faculties.update(["Право", "IT"])  # додає кілька елементів
print("Факультети:", faculties)

# Множинні операції:
a = {1, 2, 3}
b = {3, 4, 5}
print("Перетин:", a & b)       # {3}
print("Об'єднання:", a | b)    # {1, 2, 3, 4, 5}
print("Різниця:", a - b)       # {1, 2}
print("Симетрична різниця:", a ^ b) # {1, 2, 4, 5}


# =============================
# 3. К О Р Т Е Ж  (tuple)
# =============================

# Кортеж із даними студента (ім'я, вік, курс)
student_info = ("Олег", 20, 2)

# Основні методи:
print("Ім'я:", student_info[0])      # доступ по індексу
print("Кількість 'Олег':", student_info.count("Олег"))
print("Індекс віку 20:", student_info.index(20))

# Кортеж незмінний, але можна перетворити на список:
temp = list(student_info)
temp[1] = 21
student_info = tuple(temp)
print("Оновлений кортеж:", student_info)


# =============================
# 4. С Л О В Н И К  (dict)
# =============================

# Словник із оцінками студентів
grades = {
    "Олег": [90, 85, 92],
    "Марія": [100, 95],
    "Андрій": [75, 80]
}

# Основні методи:
print("Оцінки Олега:", grades.get("Олег"))  # отримати по ключу
grades["Катя"] = [88, 90]                   # додати нову пару
grades["Олег"].append(100)                  # змінити існуюче значення
print("Словник оцінок:", grades)

# Перебір словника
for name, marks in grades.items():
    print(f"{name}: середній бал = {sum(marks)/len(marks):.1f}")

# Інші методи:
print("Ключі:", grades.keys())      # усі студенти
print("Значення:", grades.values()) # усі оцінки
print("Пари:", grades.items())      # всі ключ:значення
grades.pop("Андрій")                # видаляє по ключу
grades.update({"Марія": [99, 100]}) # оновлення значень

📝 Висновок

Список → для послідовностей, часто змінюваних даних.

Множина → для унікальних елементів і роботи з множинами.

Кортеж → для незмінних структур (наприклад, координати).

Словник → для зв’язку ключ → значення (наприклад, студент → оцінки).

Хочеш, я складу тобі таблицю з усіма методами кожного типу (list, set, tuple, dict) як шпаргалку?

Ви сказали:
так
Програма ChatGPT сказала:

Чудово 👌 Тримай повну шпаргалку по методах основних колекцій у Python:

📌 Методи списків (list)
lst = [1, 2, 3, 2]

Метод	Опис	Приклад
append(x)	додає елемент в кінець	lst.append(4) → [1,2,3,2,4]
insert(i, x)	вставляє елемент на позицію i	lst.insert(1, 10) → [1,10,2,3,2]
extend(iter)	додає всі елементи іншого списку/ітератора	lst.extend([7,8])
remove(x)	видаляє перше входження елемента	lst.remove(2)
pop(i)	видаляє елемент за індексом (за замовчуванням останній)	lst.pop()
clear()	очищає список	lst.clear()
index(x)	повертає індекс першого входження	lst.index(3)
count(x)	рахує кількість входжень	lst.count(2)
sort()	сортує список	lst.sort()
reverse()	розвертає список	lst.reverse()
copy()	повертає копію	lst.copy()
📌 Методи множин (set)
s = {1, 2, 3}

Метод	Опис	Приклад
add(x)	додає елемент	s.add(4)
remove(x)	видаляє (помилка якщо нема)	s.remove(2)
discard(x)	видаляє (без помилки)	s.discard(10)
pop()	видаляє випадковий елемент	s.pop()
clear()	очищає множину	s.clear()
update(iter)	додає кілька елементів	s.update([5,6])
union(s2)	об’єднання	s.union({4,5})
intersection(s2)	перетин	s.intersection({2,3})
difference(s2)	різниця	s.difference({1,2})
symmetric_difference(s2)	симетрична різниця	s.symmetric_difference({2,4})
issubset(s2)	чи є підмножиною	s.issubset({1,2,3,4})
issuperset(s2)	чи є надмножиною	s.issuperset({1,2})
isdisjoint(s2)	чи не перетинаються	s.isdisjoint({7,8})
📌 Методи кортежів (tuple)
t = (1, 2, 2, 3)

Метод	Опис	Приклад
count(x)	рахує кількість входжень	t.count(2) → 2
index(x)	повертає індекс першого входження	t.index(3) → 3

🔹 У кортежів методів мало, бо вони незмінні. Але їх можна перетворювати на список → змінювати → знову в кортеж.

📌 Методи словників (dict)
d = {"a": 1, "b": 2}

Метод	Опис	Приклад
get(k, default)	отримати значення по ключу	d.get("a") → 1
keys()	усі ключі	d.keys()
values()	усі значення	d.values()
items()	пари ключ:значення	d.items()
pop(k)	видаляє елемент по ключу	d.pop("b")
popitem()	видаляє останню пару	d.popitem()
update(d2)	оновлює словник іншим	d.update({"c":3})
clear()	очищає словник	d.clear()
copy()	копія словника	d.copy()
setdefault(k, default)	додає ключ, якщо його нема	d.setdefault("x", 10)